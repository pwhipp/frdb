# Production email delivery through Amazon SES SMTP in ap-southeast-2.
FRDB_SES_FROM=no-reply@qclub.au
FRDB_SES_SMTP_USERNAME=replace-with-ses-smtp-username
FRDB_SES_SMTP_PASSWORD=replace-with-ses-smtp-password

# For a non-production smoke test only, use:
# FRDB_MAIL_BACKEND=console
